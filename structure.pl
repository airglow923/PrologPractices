has(albert, olive).

% below is called a functor
% the number of argumenst of a functor is called arity
female(alice).

% nested functor
eat(human, food(meat, beef)).

% eat(human, food(meat, X)).
% x = beef.